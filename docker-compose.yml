version: "3.8"

services:
  mysql:
    image: mysql:latest
    container_name: mysql_rostofirme
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - rostofirme_mysql:/var/lib/mysql
    networks:
      - rostofirme_network

networks:
  rostofirme_network:
    driver: bridge

volumes:
  rostofirme_mysql:
    driver: local